[tools]
go = "1.25.6"
"go:golang.org/x/tools/gopls" = "0.21.0"
"github:lucasew/workspaced" = "0.1.2"

[tasks.lint]
description = "Run all linters"
run = "workspaced codebase lint"

[tasks.fmt]
depends = ["fmt:*"]
description = "Format all code"
run = "workspaced codebase format"

[tasks.test]
run = "go test -v ./..."
description = "Run tests"

[tasks."build:fetchurl-image"]
run = "docker build -t fetchurl:local ."
description = "Build fetchurl Docker image (for SDK integration tests in other repos)"

[tasks.ci]
depends = ["lint", "test"]
description = "Run CI pipeline"

[tasks.install]
run = "go get ./..."
